{
  "name": "json-api-server",
  "version": "1.0.0",
  "description": "This is a dummy json based api server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "waheed khan",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
